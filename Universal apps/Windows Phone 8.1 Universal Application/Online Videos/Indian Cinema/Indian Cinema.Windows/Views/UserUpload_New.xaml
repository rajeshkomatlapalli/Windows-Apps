<Page
    x:Class="OnlineVideos.Views.UserUpload_New"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnlineVideos.Views"
    xmlns:LART="using:OnlineVideosWin81.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:UI="using:Microsoft.Advertising.WinRT.UI"
    mc:Ignorable="d"
    d:DesignHeight="850"
    d:DesignWidth="730"
    >

    <Grid Background="{StaticResource bi}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="720"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <UI:AdControl Grid.Column="0" x:Name="AdRotatorWin8" ApplicationId="{StaticResource misappid}" AdUnitId="{StaticResource misuid}" Margin="0,0,20,10" Height="600" Width="160" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <LART:UserUpload Grid.Column="1" Height="800" Width="720" HorizontalAlignment="Center" x:Name="UserUpaold"></LART:UserUpload>
        <UI:AdControl Grid.Column="2" x:Name="AdRotatorWin81" ApplicationId="{StaticResource misappid}" AdUnitId="{StaticResource misuid}" Margin="0,0,20,10" Height="600" Width="160" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
